import {
    afterEach, beforeEach, describe, it,
} from 'mocha';
import { expect } from 'chai';
import tamper from './index.js';

describe('tamper', () => {

    beforeEach(() => {
    });
    afterEach(() => {
    });
    it('TODO', () => {
        expect(1).to.eql(1);
    });
});